input:
  nats_jetstream:
    urls:
      - nats://nats:4222
    stream: orders
    subject: orders.created
    durable: redis-sink

output:
  redis_streams:
    url: tcp://redis:6379
    stream: orders-created-stream
    body_key: body
